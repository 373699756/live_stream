#include "http.h"
#include "http_dependencies.h"

int main() {
    live_stream::HttpOptions options;
    live_stream::HttpDependencies dependencies;
    (void)options;
    (void)dependencies;
    return 0;
}
