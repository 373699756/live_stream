# 按 ZLMediaKit 标准重构本工程媒体架构

## 目标

本工程不嵌入 ZLMediaKit SDK，不启动 ZLMediaKit MediaServer 旁路进程。
目标是保留本工程设备管理、HTTP 控制 API、Web Console、ONVIF 业务壳，
但按 ZLMediaKit 的媒体服务模型重构自己的媒体核心、网络会话、协议输出、
缓存和时间戳模型。

架构冲突时优先按 ZLMediaKit 的模型收敛。旧模块、类、函数、文件名如果职责
混乱、命名不标准或和 ZLM 模型冲突，可以直接改名、拆分、合并或删除。不长期
保留只转调、只兼容旧路径、只包装旧接口的适配层。

产品范围仍是 video-only：不新增音频采集、音频编码、音频传输，不新增录像、
录制、存储回放或对应 UI/API。

## ZLMediaKit 参考点

本工程参考本地源码：

- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Common/MediaSource.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Common/MultiMediaSourceMuxer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Extension/Frame.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Extension/Track.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Common/Stamp.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Record/HlsMaker.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Record/HlsMediaSource.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtmp/FlvMuxer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtsp/RtspSession.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtsp/RtspMuxer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtsp/RtpCodec.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Poller/EventPoller.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Network/TcpServer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Network/Session.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Network/Buffer.h`

可以借鉴协议算法、时间戳修正、muxer、HLS/FLV/RTSP 关键实现片段。若直接拷贝
ZLMediaKit 源码片段，必须保留 ZLMediaKit license/copyright/server 标识要求。
不要全量搬运 ZLM 工程结构，避免本工程变成另一个 MediaServer，导致配置、构建、
交叉编译和设备业务失控。

## 目标模块命名

- `device_media`：HiSilicon 编码帧采集、码流启动停止、关键帧请求。
- `media_source`：统一媒体源、MediaTrack、MediaFrame、FrameRing、GopCache、
  TimestampCorrector、浏览器协议共享媒体状态。
- `network`：EventPoller/EventLoop、TcpServer、TcpSession、Buffer、Timer。
- `http`：普通 HTTP 请求、认证、控制 API、静态资源。
- `http_media`：HLS、HTTP-FLV、媒体 WebSocket/WebRTC signaling 的 HTTP 入口。
- `rtsp`：RtspSession、RtspMuxer、RtpPacketizer、RTP TCP/UDP transport。
- `webrtc`：WebRTC signaling、ICE/DTLS/SRTP/RTP sender。
- `onvif`：ONVIF discovery/profile/PTZ 等设备协议。ZLMediaKit 不是 ONVIF 实现，
  本工程 ONVIF 继续保留，但 RTSP URL 指向本工程重构后的 RTSP 服务。

当前阶段允许先保留 `*_service` 构建规则，但 C++ public API 和热路径类型应逐步
收敛到上述语义，不再新增 `stream_hub`、`stream_mux`、`stream_codec` 这类泛名边界。

## 当前完成情况

已完成：

1. `stream_hub_service` 的媒体状态核心迁入 `libs/media_source`。
   `media_source` 已拥有 HLS/FLV 浏览器流状态、GOP cache、HLS segment、
   时间戳修正和浏览器播放 ready 状态。
2. RTSP/WebRTC 生产依赖已从旧 `stream_hub_service` 改为 `IMediaFrameSource`。
3. 生产构建新增 `libs/media_source_service`，替代 `stream_hub_service` 服务壳。
   `app`、根 `Makefile` 和相邻 HTTP 构建已经切到 `media_source_service`/`media_source`。
4. `http_service` 已去掉对 `stream_hub_service` 的生产 include/lib 依赖。
5. `libs/media_source/include/media_source.h` 已删除 `Stream*`/`IStream*` 临时兼容别名，
   生产 public API 只保留 `Media*` 命名。
6. `media_source` 已新增 `MediaFrame`、`MediaTrack`、`TimestampCorrector` 基础类型，
   现有浏览器流状态已开始使用独立 `TimestampCorrector` 修正输出 DTS/PTS。
7. 已验证 `make -C libs/media_source`、`make -C libs/media_source_service`、
   `make -C libs/http_service`、
   `make -B -C libs/rtsp_service`、`make -B -C libs/webrtc_service`、`make -j2`。

未完成：

1. 旧 `libs/stream_hub_service` 目录仍存在，生产构建不再使用，但测试和历史引用尚未清理。
2. `stream_mux` 仍承担 HLS/FLV/TS/codec mux 细节，职责过宽，需要拆成更明确的
   `hls`、`flv`、`rtp` 或 `media_mux` 内部组件。
3. `stream_codec` 仍是旧命名，应按 `media_codec` 或按 H264/H265 parser/packetizer
   职责拆分。
4. `media_source` 还没有完整的 ZLM 风格 `FrameRing`、`GopCache` 类型、
   live ring reader、统一 reader count、慢读者断开模型。
5. HTTP-FLV 仍是 HTTP handler 直接管理长连接和客户端 sink，尚未完全按 ZLM
   `FlvMuxer + live ring + GOP cache` 模型重写。
6. HLS 仍是 `media_source_stream_state` 内部 segment 状态，尚未形成独立
   `HlsMaker`/`HlsMediaSource` 生命周期。
7. RTSP 仍是现有 `rtsp_service` 会话实现，尚未完全拆成 `RtspSession`、`RtspMuxer`、
   `RtpPacketizer`、RTP transport。
8. `net_service` 还不是 ZLToolKit 风格的 `EventPoller + TcpServer + TcpSession + Buffer`
   基础层，协议模块仍有各自 socket 生命周期处理。
9. `http_service` 仍同时承载控制 API、静态文件和媒体长连接入口，尚未拆出
    `http`/`http_media` 边界。
10. WebRTC 只完成统一媒体源取帧方向的部分收敛，内部 session/transport 还没有按
    ZLM WebRTC 结构重构。
11. 内存上限、每连接发送队列上限、慢客户端主动断开、帧缓存数量统计和 RSS 观测
    还没有形成统一质量门禁。

## 是否需要借鉴 ZLMediaKit 设计

需要。当前视频内存管理、HTTP、RTSP、HTTP-FLV、HLS、模块划分、命名和函数设计
都应该继续借鉴 ZLMediaKit，但要按本工程 video-only、无异常/RTTI、交叉编译和设备业务
约束做裁剪。

### 视频内存管理

应借鉴 ZLM 的 `Frame`、`Buffer`、`RingBuffer`、GOP cache 思路：

- 统一 `MediaFrame` 引用计数对象，协议层只持有引用，不复制编码 payload。
- 入口只做一次时间戳归一化、关键帧识别和 H264/H265 参数集解析。
- `FrameRing` 承载 live 分发，`GopCache` 只缓存关键帧起始后的必要帧。
- 每路流、每协议、每连接设置缓存上限。慢客户端发送队列触顶后主动断开。
- HLS/FLV/RTSP/WebRTC 不再各自复制一套 GOP cache 或时间戳修正逻辑。

当前 `EncodedFrame`/`VideoBuffer` 已有引用语义，可以作为 `MediaFrame` 的底层基础，
但需要把 `MediaFlvCachedVideoTag` 这类协议缓存从通用媒体核心拆出去。

### 时间戳模型

应借鉴 ZLM `Stamp`/`DeltaStamp`：

- 每路 stream 维护独立 `TimestampCorrector`。
- 对外协议默认使用单调相对 DTS/PTS。
- 明确处理时间戳回退、跳变、首帧基准、DTS/PTS 排序和重置条件。
- RTSP RTP timestamp、FLV timestamp、HLS segment timestamp 都从同一 corrected stamp 派生。

当前已经做了相对时间戳和回退修正，但还没有形成独立 public 类型和跨协议统一出口。

### HTTP 框架

应借鉴 ZLToolKit `Session` 模型：

- 普通控制 HTTP 和媒体长连接 HTTP 分层。
- `HttpSession` 管连接读写、解析、keep-alive、超时、关闭。
- `HttpRouter` 只做普通 API 路由，不直接管理长连接媒体发送队列。
- HTTP-FLV、MJPEG、HLS segment 输出走 `http_media` 的 session/stream writer。
- 发送队列、写缓冲上限、慢客户端关闭在网络/session 层统一实现。

当前 `http_service` 的 handler 拆分可保留，但媒体长连接不应继续深度耦合在控制
HTTP server 内。

### HTTP-FLV

应借鉴 ZLM `FlvMuxer`：

- `FlvMuxer` 只负责 FLV header、metadata、sequence header、video tag mux。
- 数据来源是 `MediaSource` 的 live ring 和 GOP cache。
- 新客户端先拿 FLV header + sequence header + GOP cache，再进入 live ring。
- 关键帧等待、sequence header 更新、时间戳连续性在 muxer/cache 层统一处理。

当前 HTTP-FLV 客户端注册在 `media_source_service`，tag 构建仍依赖 `stream_mux` 和
`media_source_stream_state`，需要拆出独立 `flv`/`http_media` 组件。

### HLS

应借鉴 ZLM `HlsMaker`/`HlsMediaSource`：

- `HlsMaker` 管理 segment 打开、写入、flush、playlist 生成、旧 segment 删除。
- segment 只在完成后进入 playlist，可见性和删除策略明确。
- 切片边界优先跟随 IDR，避免半帧、非关键帧开头和空洞。
- HLS 状态从 `MediaSource` 派生，但 HLS segment 生命周期归 HLS 模块所有。

当前 HLS 状态已迁入 `media_source`，但仍是内部结构体，不是独立 `HlsMaker` 模型。

### RTSP

应借鉴 ZLM `RtspSession`、`RtspMuxer`、`RtpCodec`：

- `RtspSession` 负责 OPTIONS/DESCRIBE/SETUP/PLAY/TEARDOWN、认证、超时、连接关闭。
- `RtspMuxer` 负责 SDP 和 track 到 RTP ring 的转换。
- `RtpPacketizer` 负责 H264/H265 RTP 分片、seq、ssrc、timestamp、marker bit。
- RTP over TCP/UDP transport 与 session 生命周期绑定，断线时统一清理。
- 等待关键帧和 reader 计数由 `MediaSource`/ring 层提供，不在 RTSP 内重复造缓存。

当前 RTSP 已改为依赖 `IMediaFrameSource`，但内部仍未达到 ZLM session/muxer/packetizer
职责分离标准。

### WebRTC

应借鉴 ZLM WebRTC session/transport 的分层，但优先级低于 HTTP-FLV/HLS/RTSP：

- 外部 API、配置开关和前端入口保持兼容。
- 内部统一从 `MediaSource`/`FrameRing` 取帧。
- 若现有 metaRTC 集成无法满足稳定性，再继续按 session/transport/sender 分层重构。

### 模块划分和命名

应继续改：

- `media_service` 后续可逐步重命名或拆成 `device_media`，只保留设备编码入口。
- `media_source` 保留为媒体核心，但需要补齐 `MediaTrack`、`MediaFrame`、
  `FrameRing`、`GopCache`、`TimestampCorrector`。
- `media_source_service` 是过渡服务壳，后续如果只是组合 `MediaSource` 和 sink，
  应继续瘦身，避免成为新的大杂烩。
- `stream_mux` 应拆为 `hls`、`flv`、`rtp` 或私有 `media_mux` 子组件。
- `stream_codec` 应改为更明确的 H264/H265 parser/codec utils。
- `http_service` 应拆成 `http` 与 `http_media`，控制 API 和媒体长连接分离。
- `rtsp_service` 后续 public API 可保留服务概念，但内部文件和类型应改成
  `RtspSession`、`RtspMuxer`、`RtpPacketizer`、`RtpTransport`。

## 实施顺序

1. 清理 `media_source.h` 的 `Stream*` 兼容别名和旧 `stream_hub_service` 生产残留。
2. 新增 `MediaFrame`、`MediaTrack`、`FrameRing`、`GopCache`、`TimestampCorrector`
   public/internal 类型，把当前 `media_source_stream_state` 拆开。
3. 重写 HTTP-FLV：新增 `FlvMuxer` 和 live ring reader，Web Console 播放 URL 保持兼容。
4. 重写 HLS：新增 `HlsMaker`/`HlsMediaSource`，明确 segment 生命周期、playlist 可见性、
   删除策略和 IDR 边界。
5. 重构 RTSP：拆 `RtspSession`、`RtspMuxer`、`RtpPacketizer`、`RtpTransport`。
6. 重构网络基础：把 `net_service` 向 `EventPoller`、`TcpServer`、`TcpSession`、
   `Buffer`、`Timer` 收敛。
7. 拆 HTTP：控制 API 留在 `http`，HLS/FLV/WebRTC signaling 进入 `http_media`。
8. WebRTC 统一从新 `MediaSource`/`FrameRing` 取帧；效果仍不稳定时再重构 transport/session。
9. 删除旧 `stream_*` 重复代码、旧 GOP cache、旧 session 管理和临时适配层。
10. 每完成一个关键模块立即做聚焦构建、播放验证和 Conventional Commit。

## 验证计划

构建：

- `make -j2`
- `make -C libs/media_source`
- `make -C libs/media_source_service`
- `make -C libs/http_service`
- `make -C libs/rtsp_service`
- `make -C libs/stream_mux`，直到该模块被拆除
- `npm run build` in `www/`

播放：

- 浏览器 HTTP-FLV 首屏、低延迟、断线重连。
- 浏览器 HLS 连续播放、刷新、切换码流。
- VLC/ffplay RTSP TCP/UDP 拉流。
- ONVIF 客户端发现设备后，通过返回的 RTSP URL 播放。
- WebRTC 首帧、长连、断线恢复。

压测：

- 单路多客户端 HLS/FLV/RTSP 同时拉流。
- 慢客户端发送队列触顶后主动断开。
- 频繁连接/断开后 RSS、句柄数、帧缓存数量回落。
- 长时间播放检查 PTS/DTS 单调、无花屏、无 segment 空洞。
