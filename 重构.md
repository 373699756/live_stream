# 按 ZLMediaKit 标准重构媒体架构与命名体系

## 目标

本工程不嵌入 ZLMediaKit SDK，不启动 ZLMediaKit MediaServer 旁路进程。
目标是保留本工程设备管理、HTTP 控制 API、Web Console、ONVIF 业务壳，
但按 ZLMediaKit 的媒体服务模型重构自己的媒体核心、网络会话、协议输出、
缓存、时间戳模型和 WebRTC 实现。

本轮重构同时完成命名标准化：

- 模块目录和库名去掉不必要的 `_service` 后缀。
- 文件名、接口名、类名、函数名统一到本工程 C++ 风格和 ZLMediaKit 语义。
- 删除旧 `stream_*`、`MetaRtc*`、`Yang*`、只转调旧接口和只兼容旧路径的临时命名。
- WebRTC 不再保留 metaRTC，参考 ZLMediaKit `webrtc/` 自研实现，裁剪为
  video-only sendonly 预览链路。

架构冲突时优先按 ZLMediaKit 的模型收敛。旧模块、类、函数、文件名如果职责
混乱、命名不标准或和 ZLM 模型冲突，可以直接改名、拆分、合并或删除。不长期
保留适配层。

产品范围仍是 video-only：不新增音频采集、音频编码、音频传输，不新增录像、
录制、存储回放或对应 UI/API。HTTP REST 路径、Web Console DTO、配置 schema
可以跟随新架构同步重构；不为了旧调用方保留长期兼容路径或适配层。

## ZLMediaKit 参考点

本工程参考本地源码：

- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Common/MediaSource.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Common/MultiMediaSourceMuxer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Extension/Frame.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Extension/Track.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Common/Stamp.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Record/HlsMaker.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Record/HlsMediaSource.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtmp/FlvMuxer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtsp/RtspSession.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtsp/RtspMuxer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/src/Rtsp/RtpCodec.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/webrtc/WebRtcSession.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/webrtc/WebRtcTransport.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/webrtc/Sdp.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/webrtc/IceTransport.hpp`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/webrtc/DtlsTransport.hpp`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/webrtc/SrtpSession.hpp`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Poller/EventPoller.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Network/TcpServer.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Network/Session.h`
- `/home/cp/Public/hisi/live_stream_me/3rdparty/ZLMediaKit/3rdpart/ZLToolKit/src/Network/Buffer.h`

可以借鉴协议算法、时间戳修正、muxer、HLS/FLV/RTSP/WebRTC 关键实现片段。
若直接拷贝 ZLMediaKit 源码片段，必须保留 ZLMediaKit license/copyright/server
标识要求。不要全量搬运 ZLM 工程结构，避免本工程变成另一个 MediaServer，
导致配置、构建、交叉编译和设备业务失控。

## 模块命名规划

统一规则：模块目录和静态库使用业务域名，不再默认使用 `_service` 后缀。
只有确实表达生命周期外壳的内部类可以使用 `Service` 后缀，public 模块名、
目录名和库名不使用 `_service`。

| 当前模块 | 目标模块 | 说明 |
| --- | --- | --- |
| `infra_service` | `infra` | 基础工具、日志宏、通用类型。 |
| `logger_service` | `logger` | 操作日志和审计。 |
| `config_service` | `config` | JSON 配置加载、写入、订阅。 |
| `auth_service` | `auth` | 登录、token、用户认证。 |
| `event_service` | `event` | 模块事件发布订阅。 |
| `system_service` | `system` | 系统状态、重启等设备控制。 |
| `time_service` | `time` | 系统时间、NTP 配置。 |
| `network_service` | `network_config` | 网口配置和 Linux 网络状态，避免与底层 `net` 混淆。 |
| `net_service` | `net` | EventPoller/EventLoop、TcpServer、TcpSession、Buffer、Timer、UDP endpoint。 |
| `hisi_vendor` | `hisi_vendor` | HiSilicon SDK 适配层，保留现名。 |
| `media_service` | `device_media` | HiSilicon 编码帧采集、码流启停、关键帧请求。 |
| `media_source` | `media_source` | 统一媒体源、MediaFrame、MediaTrack、FrameRing、GopCache、TimestampCorrector。 |
| `media_source_service` | `media_pipeline` | `device_media` 到 `media_source` 的装配、扇出和生命周期。 |
| `stream_codec` | `media_codec` | H264/H265 parser、SPS/PPS/VPS、IDR 判断、AnnexB/AVCC 辅助。 |
| `stream_mux` | `media_mux` 或拆入协议模块 | 过渡名；FLV/HLS/RTP 稳定后删除泛名模块。 |
| `stream_hub_service` | 删除 | 不保留兼容层。 |
| `http_service` | `http` | 普通 HTTP session、认证、控制 API、静态资源。 |
| 新增 | `http_media` | HLS、HTTP-FLV、MJPEG、WebRTC signaling 的 HTTP 媒体入口。 |
| `rtsp_service` | `rtsp` | RtspSession、RtspMuxer、RtpPacketizer、RtpTransport。 |
| `webrtc_service` | `webrtc` | Native WebRTC signaling、ICE、DTLS、SRTP、RTP sender。 |
| `onvif_service` | `onvif` | ONVIF discovery/device/media/profile/PTZ 壳。 |
| `snapshot_service` | `snapshot` | 抓图配置、抓图 API 和设备抓图。 |
| `region_service` | `region` | OSD/遮挡区域配置。 |
| `ai_service` | `ai` | AI 视图、规则和事件联动。 |
| `alarm_service` | `alarm` | 告警输入、配置和事件。 |
| `upgrade_service` | `upgrade` | 升级包校验、烧写和状态机。 |

重命名时同步修改：

- `libs/<module>` 目录名。
- `SERVICE_NAME`、`module.mk`、根 `Makefile` 的 `SERVICES` 和 include path。
- public header 文件名和 include guard。
- app 组合根中的依赖字段、变量名和创建函数。
- `docs/libs/<module>.md` 文档名和内容。
- Web mock/status 里展示的模块名。

## 任务 1 命名迁移基线

本节是任务 1 的冻结结果。后续任务 2-12 只能按本节迁移命名；如果发现目标名需要
调整，必须先更新本节和 `docs/libs/README.md`，再改生产代码，避免目录、库名、
public header 和调用方出现多套临时命名。

### Public API 命名基线

通用规则：

- public header 使用目标模块名，不再使用 `*_service.h` 主接口头。
- public interface 从 `I<Module>Service` 收敛到 `I<Module>`。
- `Options`、`Dependencies`、`Stats` 去掉多余 `Service`，例如
  `RtspServiceOptions` -> `RtspOptions`。
- 工厂函数统一为 `Create<Module>()`，例如 `CreateRtspService()` -> `CreateRtsp()`。
- 变量、参数和 dependency 字段使用目标模块名，例如 `rtsp_service` -> `rtsp`。
- `Name()` / `StaticName()` 返回目标模块名，不返回旧 `*_service` 字符串。

特殊映射：

| 当前 public 名 | 目标 public 名 | 说明 |
| --- | --- | --- |
| `IMediaService` / `MediaServiceOptions` / `CreateMediaService()` | `IDeviceMedia` / `DeviceMediaOptions` / `CreateDeviceMedia()` | 设备编码入口，不再叫泛化 media service。 |
| `IMediaSourceService` / `MediaSourceServiceOptions` / `CreateMediaSourceService()` | `IMediaPipeline` / `MediaPipelineOptions` / `CreateMediaPipeline()` | 只负责 `device_media -> media_source` 装配。 |
| `INetworkService` / `NetworkServiceOptions` / `CreateNetworkService()` | `INetworkConfig` / `NetworkConfigOptions` / `CreateNetworkConfig()` | 与底层 `net` 区分。 |
| `IHttpService` / `HttpServiceOptions` / `CreateHttpService()` | `IHttp` / `HttpOptions` / `CreateHttp()` | HTTP 控制入口。 |
| `CreateHttpConsoleService()` | `CreateHttpConsole()` | Console 组合入口；HTTP REST 路径由 HTTP/http_media 任务按新架构同步重构。 |
| `IRtspService` / `RtspServiceOptions` / `CreateRtspService()` | `IRtsp` / `RtspOptions` / `CreateRtsp()` | RTSP 协议入口。 |
| `IWebrtcService` / `WebrtcServiceOptions` / `CreateWebrtcService()` | `IWebrtc` / `WebrtcOptions` / `CreateWebrtc()` | Native WebRTC 入口，同时删除 `BackendName()`。 |
| `onvif_service.h` | `onvif.h` 或现有 `onvif_server.h` | `OnvifDeviceService`、`OnvifMediaService` 属于 ONVIF 规范概念，可以保留。 |

`NetEngine`、`IAuthUserStore`、`IPasswordVerifier`、`RequestContext`、
`HttpHandler`、`OperationLogStore`、配置回滚对象等已有明确业务语义的名称不因为含有
`Engine`、`Store`、`Context`、`Handler` 字样而强制重命名。

### 破坏式重构边界

本轮以完全重构为目标，不要求保持旧 HTTP REST 路径、Web Console DTO、配置
schema、事件 payload 或旧 public API 兼容。允许破坏式变更，但必须在同一任务内
同步更新所有生产调用方、前端、配置样例、文档和验收说明，不能留下新旧两套长期并存。

允许重构：

- HTTP REST 路径可以按 `http`、`http_media`、`webrtc`、`rtsp` 等新模块边界重新组织。
- Web Console DTO 字段、配置 JSON schema、认证用户配置和事件 payload 可以按新命名
  和新职责重排。
- ONVIF 必须继续符合协议标准，但本工程返回的 RTSP URL 路径、模块接口和内部对象名
  可以随 `rtsp`/`onvif` 重构同步修改。
- 过渡期构建验证可以使用当前目录名；生产接口迁移完成后不保留旧 public wrapper、
  旧 REST route alias 或旧 DTO 适配层。

必须删除或重命名：

- `stream_hub_service` 旧兼容层、`StreamHub*` typedef、只转调旧接口的 wrapper。
- `stream_codec`、`stream_mux` 旧模块名；分别收敛到 `media_codec`、`media_mux` 或按协议拆入边界模块。
- `MetaRtc*`、`Yang*` 类型、include、链接库和 public `BackendName()`。
- 旧 `*_service.h` 主接口头、旧 `Create*Service()` 工厂、旧 `I*Service` public interface。
- 只为了兼容旧路径、旧变量名、旧 DTO 或旧库名存在的中间 helper。

### 旧命名扫描项

生产代码扫描优先看 `app/`、`libs/`、`Makefile`、`www/src` 和 `www/README.md`：

```sh
rg -n 'libs/[a-z_]+_service|lib[a-z_]+_service|#include ".*_service\.h"|I[A-Za-z]+Service|Create[A-Za-z]+Service|[A-Za-z]+Service(Options|Dependencies|Stats)|BackendName\(|MetaRtc|metaRTC|Yang|stream_hub|stream_mux|stream_codec' app libs Makefile www/src www/README.md
```

文档扫描允许历史迁移说明保留旧名，但必须明确标注为旧名称或删除目标：

```sh
rg -n '_service|stream_hub|stream_mux|stream_codec|MetaRtc|metaRTC|Yang|BackendName\(' docs 重构.md
```

任务 12 之前允许旧名出现在待迁移模块和历史说明中；任务 12 验收时，生产代码旧名只允许
出现在协议规范概念、测试 fixture 或明确的历史迁移文档里。

## 命名标准

### 文件命名

- public header 使用模块主名，例如 `webrtc.h`、`rtsp.h`、`http.h`、
  `media_pipeline.h`。
- private 文件使用职责明确的 snake_case，例如 `webrtc_session.cpp`、
  `ice_transport.cpp`、`rtp_packetizer.cpp`、`http_stream_writer.cpp`。
- 不用 `*_service.cpp` 表示模块内部实现，除非文件确实只包含生命周期外壳。
- 删除或重命名 `stream_*`、`webrtc_engine.*`、`webrtc_transport_net.*` 这类
  旧实现命名。

### 类型命名

- class/struct/enum 使用 `PascalCase`。
- 接口类继续保留现有 `I` 前缀以降低全工程震荡，但去掉多余 `Service`：
  - `IWebrtcService` -> `IWebrtc`
  - `IRtspService` -> `IRtsp`
  - `IHttpService` -> `IHttp`
  - `IMediaSourceService` -> `IMediaPipeline`
- 生命周期外壳类允许叫 `WebrtcService`、`RtspService`，但优先作为内部实现类。
- 删除 `MetaRtcEngine`、`MetaRtcPeerSession`、`MetaRtcPeerCallbacks`、`Yang*`
  命名和相关抽象。
- 避免泛名 `Manager`、`Context`、`Store`、`Target`、`Data`。已有
  `RequestContext`、配置回滚、HTTP handler 等业务语义明确的名称可以保留。

### 函数和变量命名

- public C++ 函数继续 `PascalCase`。
- 工厂函数统一为 `Create<Module>()`：
  - `CreateWebrtcService()` -> `CreateWebrtc()`
  - `CreateRtspService()` -> `CreateRtsp()`
  - `CreateHttpConsoleService()` -> `CreateHttpConsole()`
  - `CreateMediaSourceService()` -> `CreateMediaPipeline()`
- 变量和参数使用 `snake_case`，并同步去掉 `_service`：
  - `webrtc_service` -> `webrtc`
  - `rtsp_service` -> `rtsp`
  - `media_source_service` -> `media_pipeline`
- 模块 `Name()` 返回新模块名：`"webrtc"`、`"rtsp"`、`"http"`，不再返回
  `"*_service"`。
- 有副作用的流程按步骤写清楚，不用 `ok = a && b` 压缩生命周期、配置、
  IO 或硬件调用。

## 当前完成情况

已完成：

1. 任务 1 命名迁移基线已冻结，模块名、public API 目标名、破坏式重构边界、
   WebRTC native 方向和旧命名扫描项已明确。
2. 任务 2 物理命名迁移已完成。当前 `libs/` 顶层目录已切到新模块名，
   不再保留计划删除的 `*_service` 模块目录，`stream_hub_service` 兼容目录已删除。
3. `docs/libs` 模块文档已按新模块名改为 `docs/libs/<module>.md`，
   `docs/libs/README.md` 和 `docs/README.md` 已同步新索引。
4. `stream_hub_service` 的媒体状态核心迁入 `libs/media_source`。
   `media_source` 已拥有 HLS/FLV 浏览器流状态、GOP cache、HLS segment、
   时间戳修正和浏览器播放 ready 状态。
5. RTSP/WebRTC 生产依赖已从旧 `stream_hub_service` 改为 `IMediaFrameSource`。
6. `libs/media_source/include/media_source.h` 已删除 `Stream*`/`IStream*` 临时兼容别名，
   生产 public API 只保留 `Media*` 命名。
7. `media_source` 已新增 `MediaFrame`、`MediaTrack`、`TimestampCorrector` 基础类型，
   现有浏览器流状态已开始使用独立 `TimestampCorrector` 修正输出 DTS/PTS。
8. `media_source` 已新增内部 `GopCache`、`FrameRing`、`FlvMuxer` 和 `FlvLiveRing`
   基础类型，HTTP-FLV/GOP/帧分发已有部分逻辑下沉到媒体核心。

未完成：

1. 任务 3 尚未完成：public header、接口类、Options/Dependencies/Stats、工厂函数、
   app 组合根变量和调用方仍需统一去掉旧 `Service` 命名。
2. `media_source` 的 live reader 还没有作为稳定 public 订阅接口暴露。
3. `media_codec` 仍需从旧 `stream_codec` 迁移后的结构整理为 H264/H265 parser、
   参数集、IDR 判断和 AnnexB/AVCC 工具边界。
4. `media_mux` 仍是过渡模块，FLV/HLS/RTP mux 细节后续应拆入 `http_media`、
   `rtsp`、`webrtc` 或明确的 codec/RTP 边界。
5. HTTP-FLV/HLS/MJPEG 还没有完全拆到 `http_media` session/stream writer。
6. RTSP 仍未完全拆成 `RtspSession`、`RtspMuxer`、`RtpPacketizer`、`RtpTransport`。
7. `net` 还不是 ZLToolKit 风格的基础网络层，协议模块仍有各自 socket
   生命周期处理。
8. WebRTC 当前仍绑定 metaRTC/Yang API，需要替换为 native WebRTC。
9. 内存上限、每连接发送队列上限、慢客户端主动断开、帧缓存数量统计和 RSS 观测
    还没有形成统一质量门禁。

## 媒体核心设计

### 视频内存管理

借鉴 ZLM 的 `Frame`、`Buffer`、`RingBuffer`、GOP cache 思路：

- 统一 `MediaFrame` 引用计数对象，协议层只持有引用，不复制编码 payload。
- 入口只做一次时间戳归一化、关键帧识别和 H264/H265 参数集解析。
- `FrameRing` 承载 live 分发，`GopCache` 只缓存关键帧起始后的必要帧。
- 每路流、每协议、每连接设置缓存上限。慢客户端发送队列触顶后主动断开。
- HLS/FLV/RTSP/WebRTC 不再各自复制 GOP cache 或时间戳修正逻辑。

当前 `EncodedFrame`/`VideoBuffer` 已有引用语义，可以作为 `MediaFrame` 的底层基础。
`MediaFlvCachedVideoTag` 这类协议缓存后续应从通用媒体核心拆到 FLV 边界。

### 时间戳模型

借鉴 ZLM `Stamp`/`DeltaStamp`：

- 每路 stream 维护独立 `TimestampCorrector`。
- 对外协议默认使用单调相对 DTS/PTS。
- 明确处理时间戳回退、跳变、首帧基准、DTS/PTS 排序和重置条件。
- RTSP RTP timestamp、WebRTC RTP timestamp、FLV timestamp、HLS segment timestamp
  都从同一 corrected stamp 派生。

### MediaSource reader

`media_source` 应稳定暴露统一 reader 模型：

- 新 reader 可选择 keyframe-first。
- reader 可获取启动数据、当前 GOP、live frame。
- reader count、缓存帧数、缓存字节数可统计。
- codec 切换、stream stop、timestamp reset 会通知或关闭 reader。
- RTSP/WebRTC/HTTP-FLV/MJPEG 不再各自维护重复 GOP 和关键帧等待逻辑。

## 协议模块设计

### HTTP 与 HTTP Media

借鉴 ZLToolKit `Session` 模型：

- `http` 负责普通 HTTP session、解析、keep-alive、认证、控制 API、静态文件。
- `http_media` 负责 HTTP-FLV、HLS、MJPEG、WebRTC signaling 的媒体输出和长连接。
- `HttpRouter` 只做普通 API 路由，不直接管理长连接媒体发送队列。
- `HttpSession` 管连接读写、解析、keep-alive、超时、关闭。
- 发送队列、写缓冲上限、慢客户端关闭在 `net`/session 层统一实现。

HTTP REST 路径可以按新模块边界重排；例如 WebRTC signaling、HTTP-FLV、HLS、
MJPEG 可以统一归 `http_media` 路由。路径变更时同步更新 Web Console、配置样例和文档，
不保留旧 route alias。

### HTTP-FLV

借鉴 ZLM `FlvMuxer`：

- `FlvMuxer` 只负责 FLV header、metadata、sequence header、video tag mux。
- 数据来源是 `media_source` 的 live reader 和 GOP cache。
- 新客户端先拿 FLV header + sequence header + GOP cache，再进入 live ring。
- 关键帧等待、sequence header 更新、时间戳连续性在 muxer/cache 层统一处理。

### HLS

借鉴 ZLM `HlsMaker`/`HlsMediaSource`：

- `HlsMaker` 管理 segment 打开、写入、flush、playlist 生成、旧 segment 删除。
- segment 只在完成后进入 playlist，可见性和删除策略明确。
- 切片边界优先跟随 IDR，避免半帧、非关键帧开头和空洞。
- HLS 状态从 `media_source` 派生，但 HLS segment 生命周期归 HLS 模块所有。

### RTSP

借鉴 ZLM `RtspSession`、`RtspMuxer`、`RtpCodec`：

- `RtspSession` 负责 OPTIONS/DESCRIBE/SETUP/PLAY/TEARDOWN、认证、超时、连接关闭。
- `RtspMuxer` 负责 SDP 和 track 到 RTP ring 的转换。
- `RtpPacketizer` 负责 H264/H265 RTP 分片、seq、ssrc、timestamp、marker bit。
- `RtpTransport` 负责 RTP over TCP/UDP transport，与 session 生命周期绑定。
- 等待关键帧和 reader 计数由 `media_source`/ring 层提供，不在 RTSP 内重复造缓存。

### WebRTC

WebRTC 不再保留 metaRTC。目标是 native video-only sendonly WebRTC 预览链路，
参考 ZLMediaKit `webrtc/` 分层，但按本工程无异常/RTTI、交叉编译和嵌入式内存约束裁剪。

删除范围：

- `metaRTC_src` include 依赖。
- `libmetartc8.a`、`libmetartccore8.a`、`libyangutil8.a` 链接依赖。
- `MetaRtcEngine`、`MetaRtcPeerSession`、`MetaRtcPeerCallbacks`。
- 所有 `Yang*` 类型、回调和 pacer 调用。
- `BackendName()` 这类只暴露后端实现名称的 public API。

目标文件和职责：

- `webrtc_sdp.*`：SDP offer parse、answer generate、H264/H265 payload、ICE ufrag/pwd、
  DTLS fingerprint、candidate。
- `stun_packet.*`：STUN binding request/response、ICE username、message integrity、
  fingerprint。
- `ice_transport.*`：host candidate、candidate pair、connectivity check、selected pair。
- `dtls_transport.*`：OpenSSL DTLS handshake、certificate fingerprint、SRTP key export。
- `srtp_session.*`：libsrtp RTP/RTCP encrypt。
- `webrtc_transport.*`：ICE + DTLS + SRTP 组合，负责包输入输出和连接状态。
- `webrtc_session.*`：peer 生命周期、offer/answer/candidate/close、timeout。
- `webrtc_rtp_sender.*`：从 `MediaFrameReader` 取帧，H264/H265 RTP packetize，
  PLI 后请求关键帧。
- `nack.*`、`twcc_context.*`：首版只保留必要结构；NACK/TWCC 可在基本播放稳定后实现。

首版能力：

- video-only sendonly。
- 优先 H264；H265 根据浏览器支持和现有能力字段再打开。
- ICE UDP host candidate 优先；TURN/TCP 可后续补齐。
- 不实现 audio、datachannel、推流、录制、存储回放。
- HTTP WebRTC signaling 契约按 native `webrtc` 重构，可以改路径和 JSON 字段；
  变更时同步更新 `http_media`、Web Console 和文档，不保留 metaRTC 兼容字段。

### ONVIF

ZLMediaKit 不是 ONVIF 实现，本工程 ONVIF 继续保留。ONVIF 模块只负责 discovery、
device、media/profile、PTZ 等设备协议壳，返回的 RTSP URL 指向本工程重构后的
`rtsp` 服务。ONVIF 不拥有媒体缓存、RTP packetizer 或 WebRTC 状态。

## 执行总顺序与并行规则

任务编号是主顺序；“冻结基础契约”不是另一套流程，而是任务 4、5、6、8 的合入门禁。
任务 1、2 已完成，后续从任务 3 开始执行。

推荐并行顺序：

```text
任务 3 串行 -> 任务 4/5/6/8 并行 -> 任务 7/9/10/11 并行 -> 任务 12 串行
```

人手少时使用保守串行顺序：

```text
任务 3 -> 任务 4 -> 任务 5 -> 任务 6 -> 任务 8 -> 任务 7 ->
任务 9 -> 任务 10 -> 任务 11 -> 任务 12
```

### 必须串行

- 任务 3：public header、接口名、工厂函数和 app 组合根统一迁移。只能一个 Codex
  处理，或一个 Codex 统一 review 后合入。
- 任务 2 是历史串行任务，已完成；后续 Codex 不再重新执行目录和静态库重命名。
- 任务 12：最终删除旧目录、旧 public header、旧库名、metaRTC/Yang 残留和临时 wrapper。
  必须等任务 3-11 全部完成后执行。

### 可以并行

- 第一批：任务 4、5、6、8。
  - 任务 4/5：冻结 `device_media -> media_pipeline -> media_source` 和
    `MediaFrameReader`、GOP、时间戳、reader count。
  - 任务 6：冻结 `net` 的 session、send queue、buffer limit、timer、TCP/UDP endpoint。
  - 任务 8：冻结 `media_codec` parser 和 RTSP/WebRTC 共用 RTP packetizer。
- 第二批：任务 7、9、10、11。
  - 任务 7：`http`/`http_media`。
  - 任务 9：`rtsp`。
  - 任务 10：native `webrtc`，不保留 metaRTC。
  - 任务 11：`onvif`，只依赖 RTSP URL、认证和设备配置。

第二批必须等任务 5、6 的接口稳定后再合入；RTSP/WebRTC 还必须等任务 8 的
codec/RTP packetizer 接口稳定后接入。

### 并行禁改规则

- 并行任务只能改自己的模块和明确依赖的调用方；任务 3 之外不能顺手做全局 rename。
- 任务 7、9、10 不得新增私有 GOP cache、私有 socket 队列或私有时间戳修正。
- RTSP 和 WebRTC 必须复用任务 8 的 H264/H265 parser/RTP packetizer，不能各写一套。
- WebRTC 任务不能改 `media_source`、`net`、`media_codec` public API。
- 每个任务合入前至少跑本模块构建；改了共享接口时额外跑相邻模块构建。

### 文档同步规则

- 每个任务改目录、接口、配置、HTTP API、Web Console DTO 或模块职责时，必须在同一任务内
  同步更新 `docs/` 及其子目录中的对应设计文档。
- 模块任务同步更新 `docs/libs/<module>.md`；模块重命名时同步更新文档文件名、
  `docs/libs/README.md` 和 `docs/README.md` 索引。
- Web Console、HTTP API、DTO 或前端调用变化同步更新 `docs/web/web-console-design.md`。
- HTTP 完全重构时允许同任务修改 `www/`，同步迁移前端 API client、mock 数据、页面调用
  和类型定义；这属于任务 7 范围，不算跨模块越界。
- 内存上限、缓存策略、发送队列、慢客户端断开和资源回落门禁变化同步更新
  `docs/optimization/memory.md`。
- 当前 `docs/` 目录只有 `README.md`、`libs/`、`optimization/`、`web/`；不要引用
  尚不存在的 `docs/active/`、`docs/contracts/`、`docs/features/` 或 `docs/quality/`。
- 任务验收时同时检查生产代码和文档旧名扫描结果；除历史迁移说明外，设计文档不得继续使用
  已删除的旧模块名、旧接口名或旧 API 契约。

### Review 与验收规则

- 每个任务或子任务实现完成后，Codex 必须自动做一次代码 review，再执行该任务列出的验收步骤。
- Review 重点检查模块边界、命名标准、旧兼容残留、资源释放、线程/生命周期、错误路径、
  文档同步和是否误改其他 Codex 负责的文件。
- 验收步骤必须按任务说明执行；文档任务可用 diff 和旧名扫描验收，代码任务至少跑本模块构建，
  跨接口任务额外跑相邻模块构建，前端改动跑 `npm run build`。
- 发现 review 问题或验收失败时，先修复并重新验收；不能带着已知失败提交。
- 最终回复或提交说明中必须记录已执行的验收命令、结果，以及未能执行的验收项和原因。

### 提交与推送规则

- 每完成一个可独立验收步骤后，必须立即提交并推送到 GitHub；不要把多个无关步骤堆到同一个提交。
- 提交前必须完成本步骤 review、验收、文档同步和 `git status --short` 检查，只 stage 当前步骤相关文件。
- 提交信息使用 Conventional Commits，并包含任务号或子任务号，例如
  `refactor(webrtc): task 10.3 rebuild native sdp`。
- 推送使用当前工作分支对应的 GitHub remote，前提是 remote、凭据和当前分支状态允许安全 push。
  如果 push 失败，记录原因，不强推，不把本地已通过验收的提交回滚。
- 如果 push 被拒绝，先同步远端并解决冲突；没有用户明确授权时不得 force push
  覆盖其他 Codex 或用户提交。
- 多 Codex 并行时，每个 Codex 只提交自己负责的模块、相邻调用方和对应文档；不得把其他
  Codex 的未提交修改一起提交。
- 工作区存在其他 Codex 的未提交修改时，使用精确 pathspec stage；无法隔离本步骤文件时，
  先停止提交并在回复中说明阻塞原因。

### ZLMediaKit 对齐要求

- 相关模块设计默认以 ZLMediaKit 对应实现为第一参考，而不是只借名称。
- `media_source` 对齐 `MediaSource`、`Frame`、`Track`、`Stamp`、ring/GOP cache。
- `http`/`net` 对齐 ZLToolKit `Session`、`TcpServer`、`Buffer`、`EventPoller`。
- `rtsp` 对齐 `RtspSession`、`RtspMuxer`、`RtpCodec`。
- `webrtc` 对齐 ZLM `WebRtcSession`、`WebRtcTransport`、`Sdp`、`IceTransport`、
  `DtlsTransport`、`SrtpSession`、`StunPacket`、`Nack`、`TwccContext`，但按本工程
  video-only、无异常/RTTI、无 audio/datachannel 的范围裁剪。
- 如果本工程现有实现和 ZLM 模型冲突，优先收敛到 ZLM 模型；只有硬件、交叉编译、
  内存上限或产品范围明确冲突时才裁剪。

## 任务拆解

以下每个任务执行时，都必须同步更新当前 `docs/` 结构中的对应设计文档和索引：
`docs/README.md`、`docs/libs/`、`docs/optimization/memory.md`、
`docs/web/web-console-design.md`。文档同步是任务验收条件，不放到任务 12 统一补。

### 任务 1：冻结命名规范和迁移边界

工作内容：

- 固定模块命名表、文件命名、接口命名、工厂函数命名和变量命名规则。
- 明确哪些旧路径、旧 DTO、旧 schema、旧 public API 和兼容层必须删除。
- 在 `重构.md`、`docs/libs/<module>.md` 和模块 README 中同步命名规则。
- 列出全工程旧命名扫描项：`*_service`、`stream_*`、`MetaRtc*`、`Yang*`、
  `BackendName()`、只转调旧接口的 wrapper。

命名修改：

- 模块名按业务域命名，不再默认使用 `_service`。
- public interface 从 `I*Service` 收敛到 `I*`。
- 工厂函数从 `Create*Service()` 收敛到 `Create*()`。
- 变量名去掉 `_service` 后缀，例如 `webrtc_service` 改为 `webrtc`。

框架修改：

- 暂不改生产代码框架，只固定迁移规则和禁止新增旧命名。
- 后续任务所有重命名都以本任务为准，不再临时发明新名字。

意义：

- 避免多人并行时各自使用不同命名标准。
- 先把“哪些旧兼容必须删除、哪些调用方必须同步迁移”说清楚，减少反复适配。

验收：

- 文档中存在完整模块迁移表。
- 文档明确 WebRTC 不再保留 metaRTC。
- 后续提交不再新增旧 `stream_*` 或新的 `*_service` 模块名。

完成状态：

- 已完成。命名表、public API 命名规则、破坏式重构边界、可删除兼容层和旧名
  扫描项已冻结在“任务 1 命名迁移基线”。
- `docs/libs/README.md` 和各模块设计文档已同步要求后续重命名以本任务为准。
- 本任务不修改生产代码框架；目录、库名和 public header 的实际迁移由任务 2、3 执行。

### 任务 2：模块目录、静态库和构建系统重命名

- 推荐 Codex：Codex-0 Integration/Naming。
- 前置依赖：任务 1 命名迁移基线已冻结。
- 可并行任务：无；这是全局 rename 任务。
- 禁止同时修改：其他 Codex 不得同时改根 `Makefile`、`module.mk`、模块目录名、静态库名。

工作内容：

- 按模块命名表重命名 `libs/<module>` 目录。
- 修改每个模块 `Makefile` 的 `SERVICE_NAME`。
- 修改每个模块 `module.mk` 中的库注册名。
- 修改根 `Makefile` 的 `SERVICES`、include path、依赖库名和链接顺序。
- 修改 `libs/service_rules.mk` 中仍假设旧目录名或旧 include path 的逻辑。
- 分批处理，每批保证模块可单独构建。

命名修改：

- `libs/net_service` -> `libs/net`，库名 `libnet_service.a` -> `libnet.a`。
- `libs/http_service` -> `libs/http`，库名 `libhttp_service.a` -> `libhttp.a`。
- `libs/rtsp_service` -> `libs/rtsp`，库名 `librtsp_service.a` -> `librtsp.a`。
- `libs/webrtc_service` -> `libs/webrtc`，库名 `libwebrtc_service.a` -> `libwebrtc.a`。
- `libs/media_source_service` -> `libs/media_pipeline`，库名改为 `libmedia_pipeline.a`。
- `libs/media_service` -> `libs/device_media`，库名改为 `libdevice_media.a`。
- `libs/stream_codec` -> `libs/media_codec`。
- `libs/stream_mux` 先改为 `libs/media_mux`，后续按协议拆除。
- 业务模块同步去后缀，例如 `auth_service` -> `auth`、`config_service` -> `config`。

框架修改：

- 构建系统从“服务后缀目录列表”改为“业务模块列表”。
- app 组合根只依赖新模块名。
- 不保留长期旧目录和旧库名；迁移提交内同步更新调用方。

意义：

- 先把工程物理边界清干净，后续代码重构不再被旧库名和旧 include path 牵制。
- 静态库名和模块职责一致，链接错误更容易定位。

验收：

- `find libs -maxdepth 1 -type d` 不再出现计划删除的 `*_service` 模块目录。
- 根 `Makefile` 的 `SERVICES` 使用新模块名。
- 每批迁移后可执行 `make -C libs/<new_module>`。

完成状态：

- 已完成。`libs/` 物理目录已按命名表迁移，`stream_hub_service` 兼容目录已删除。
- 根 `Makefile` 的 include path、`SERVICES`、静态库名和 `libinfra.a` 链接名已切到
  新模块名。
- 各模块 `SERVICE_NAME`、`module.mk` 注册名、模块 Makefile 的 include path、
  `EXTRA_TEST_DEPS` / `EXTRA_TEST_LIBS` 和相邻模块 `make -C` 路径已切到新模块名。
- `libs/service_rules.mk` 的通用 infra include path 已切到 `libs/infra/include`。
- 验证通过：旧目录/旧静态库名扫描为空；`make -C libs/infra`、
  `make -C libs/media_codec`、`make -C libs/net`、`make -C libs/config`、
  `make -C libs/device_media`、`make -C libs/media_mux`、
  `make -C libs/media_source`、`make -C libs/media_pipeline`、
  `make -C libs/rtsp`、`make -C libs/webrtc`、`make -C libs/onvif`、
  `make -C libs/http` 通过。
- public header、接口类、工厂函数和变量名仍按任务 3 处理，本任务不提前改
  `*_service.h`、`I*Service` 或 `Create*Service()`。

### 任务 3：public header、接口类、工厂函数和变量命名收敛

- 推荐 Codex：Codex-0 Integration/Naming。
- 前置依赖：任务 2 完成并能构建基础模块。
- 可并行任务：无；这是 public API 基线任务。
- 禁止同时修改：其他 Codex 不得同时改主 public header、接口类名、工厂函数和 app 组合根字段。

工作内容：

- 重命名 public header：`webrtc_service.h` -> `webrtc.h`、`rtsp_service.h` -> `rtsp.h`、
  `http_service.h` -> `http.h`、`media_source_service.h` -> `media_pipeline.h`。
- 修改 include guard，使用 `LIVE_STREAM_<MODULE>_<FILE>_H_`。
- 修改接口类、options、dependencies、stats、factory 和 app 持有变量。
- 修改 HTTP handler、app subsystem、docs、Web mock 中展示的模块名。

命名修改：

- `IWebrtcService` -> `IWebrtc`。
- `WebrtcServiceOptions` -> `WebrtcOptions`。
- `WebrtcServiceDependencies` -> `WebrtcDependencies`。
- `WebrtcServiceStats` -> `WebrtcStats`。
- `CreateWebrtcService()` -> `CreateWebrtc()`。
- `IRtspService` -> `IRtsp`，`RtspServiceOptions` -> `RtspOptions`，
  `CreateRtspService()` -> `CreateRtsp()`。
- `IHttpService` -> `IHttp`，`CreateHttpConsoleService()` -> `CreateHttpConsole()`。
- `IMediaSourceService` -> `IMediaPipeline`，
  `CreateMediaSourceService()` -> `CreateMediaPipeline()`。

框架修改：

- public API 只保留新接口，不增加旧接口转调。
- app 组合根字段同步改为 `webrtc_`、`rtsp_`、`http_`、`media_pipeline_`。
- dependency struct 字段同步改名，例如 `webrtc_service` -> `webrtc`。

意义：

- 让调用方看到的是业务能力，而不是旧“service 壳”。
- 避免新旧接口并存导致后续实现分叉。

验收：

- 生产代码中不再 include `*_service.h` 主接口头。
- 生产代码中不再出现 `IWebrtcService`、`IRtspService`、`CreateWebrtcService()` 等旧符号。
- `Name()` 返回新模块名，例如 `"webrtc"`。

### 任务 4：媒体入口重命名为 `device_media`，装配层重命名为 `media_pipeline`

- 推荐 Codex：Codex-1 Media。
- 前置依赖：任务 3 完成。
- 可并行任务：任务 5、任务 6；与任务 8 可并行但不要改 codec/RTP 接口。
- 禁止同时修改：不要改 HTTP、RTSP、WebRTC 协议实现；只固定媒体入口和装配主链路。

工作内容：

- 将当前 `media_service` 中的 HiSilicon 编码、码流启停、关键帧请求职责收敛到
  `device_media`。
- 将当前 `media_source_service` 改为 `media_pipeline`，只负责连接 `device_media`
  和 `media_source`。
- 删除 `media_pipeline` 中重复缓存、重复 GOP、重复时间戳修正。
- 明确 `device_media` 不知道 HTTP、RTSP、WebRTC、HLS、FLV。

命名修改：

- `IMediaService` -> `IDeviceMedia`。
- `MediaServiceOptions` -> `DeviceMediaOptions`。
- `CreateMediaService()` -> `CreateDeviceMedia()`。
- `IMediaSourceService` -> `IMediaPipeline`。
- `MediaSourceServiceOptions` -> `MediaPipelineOptions`。
- `MediaSourceServiceDependencies` -> `MediaPipelineDependencies`。
- `CreateMediaSourceService()` -> `CreateMediaPipeline()`。

框架修改：

- 数据流固定为 `device_media -> media_pipeline -> media_source`。
- `device_media` 输出 `EncodedFrame` 或后续 `MediaFrame` 基础数据。
- `media_pipeline` 只处理生命周期、订阅设备帧、写入 `media_source`、请求关键帧。
- 下游协议只依赖 `media_source` reader，不依赖 `device_media`。

意义：

- 把硬件设备入口和协议分发切开。
- 设备采集变更不会影响 HTTP/RTSP/WebRTC。
- 协议模块可以并行开发，因为它们只依赖稳定的 `media_source`。

验收：

- RTSP/WebRTC/HTTP 不直接 include `device_media` 主接口，除非是控制 API 查询设备能力。
- `media_pipeline` 不再成为新的大杂烩，不拥有协议长连接和 socket。
- 停止码流时 `media_source` ready、reader、GOP、HLS/FLV 状态能被统一重置。

### 任务 5：冻结 `media_source` reader、缓存和时间戳接口

- 推荐 Codex：Codex-1 Media。
- 前置依赖：任务 3 完成；任务 4 的主链路方向已确定。
- 可并行任务：任务 6；接口冻结后解除任务 7、9、10 的阻塞。
- 禁止同时修改：协议 Codex 不得在此期间新增私有 GOP cache、私有时间戳修正或替代 reader。

工作内容：

- 将 `MediaFrame`、`MediaTrack`、`TimestampCorrector` 作为稳定 public 或半 public
  类型整理出来。
- 把 `FrameRing` 的 live reader 暴露给 RTSP/WebRTC/HTTP-FLV/MJPEG。
- 统一 keyframe-first、reader count、慢读者统计和 detach 生命周期。
- 将 GOP cache、FLV sequence header、HLS segment、MJPEG latest frame 的职责拆清。
- 在 codec 切换、时间戳跳变、码流停止时统一清理缓存和通知 reader。

命名修改：

- 保留 `MediaFrame`、`MediaTrack`、`TimestampCorrector`、`FrameRing`、`GopCache`。
- 新 reader 命名为 `MediaFrameReader` 或 `FrameRingReader`，不要叫 `StreamReader`。
- `MediaFlvCachedVideoTag` 后续移动到 FLV 边界，避免通用核心出现协议缓存名。
- `StreamId` 如暂时保留，应只表达主/子码流业务事实，不再派生 `Stream*` 旧接口名。

框架修改：

- 协议模块从“注册 sink 回调”逐步改为“持有 reader 拉取/接收媒体帧”。
- 所有协议时间戳从同一个 corrected stamp 派生。
- `media_source` 只拥有媒体状态，不拥有 socket 和 HTTP/RTSP/WebRTC session。

意义：

- 多协议共享同一帧引用和同一 GOP 起点，减少复制和缓存峰值。
- RTSP、WebRTC、HTTP-FLV、HLS 可以并行实现，不重复造关键帧等待逻辑。

验收：

- 新增协议输出不需要自己维护 GOP cache。
- reader count 能准确反映 RTSP/WebRTC/HTTP-FLV/MJPEG 活跃订阅。
- 慢读者断开后帧引用能回落。

### 任务 6：`net` 基础层收敛为 EventPoller/TcpSession/Buffer/Timer

- 推荐 Codex：Codex-2 Net。
- 前置依赖：任务 3 完成。
- 可并行任务：任务 4、任务 5；接口冻结后解除任务 7、9、10 的阻塞。
- 禁止同时修改：协议 Codex 不得绕过 `net` 新增私有 socket send queue、timer 或 UDP endpoint 生命周期。

工作内容：

- 将 `net_service` 重命名为 `net`。
- 明确 `EventLoop`/`EventPoller`、`TcpServer`、`TcpSession`、`Buffer`、`Timer`、
  `UdpEndpoint` 的职责。
- 下沉发送队列容量、pending bytes、写超时、close reason、manager tick。
- 提供统一 TCP/UDP 生命周期接口给 HTTP、RTSP、WebRTC、ONVIF。

命名修改：

- `NetEngine` 可保留为业务明确的网络引擎，或收敛为 `NetPoller`/`NetRuntime`，
  但不要新增泛名 `NetworkManager`。
- `TcpConnection` 若承担 session 语义，应改为 `TcpSession`。
- `send_buffer_limit_bytes`、`send_queue_capacity`、`send_stall_timeout_ms` 等字段
  在 HTTP/RTSP/WebRTC 中统一命名。

框架修改：

- 协议模块不再各自实现不可比较的 socket 写队列。
- 所有长连接统一通过 `net` 的写限制和关闭回调治理慢客户端。
- UDP RTP、ICE/STUN、ONVIF discovery 通过明确 UDP endpoint 生命周期管理。

意义：

- 慢客户端处理统一，避免 HTTP/RTSP/WebRTC 内存策略互相不一致。
- 后续性能压测可以从 `net` 统一观察 pending bytes、fd、连接数。

验收：

- HTTP/RTSP/WebRTC 不再直接绕过 `net` 管理长连接发送队列。
- 慢客户端触顶后能主动关闭并释放 media reader。
- 停止协议模块时连接和 timer 能按顺序释放。

### 任务 7：拆 `http` 和 `http_media`

- 推荐 Codex：Codex-4 HTTP。
- 前置依赖：任务 5、任务 6；HLS/FLV mux 边界依赖任务 8。
- 可并行任务：任务 9、任务 10、任务 11。
- 禁止同时修改：不要改 `media_source` reader、`net` session 公共接口或 WebRTC transport 内部状态。

工作内容：

- 将普通 HTTP session、认证、控制 API、静态文件保留在 `http`。
- 将 HLS、HTTP-FLV、MJPEG、WebRTC signaling 的媒体输出移动到 `http_media`。
- 按新 `http`/`http_media` 边界重构 REST 路径和 DTO，并同步更新 Web Console 调用。
- 如果 REST 路径、DTO 或认证/状态响应发生变化，同步修改 `www/` 的 API client、
  mock 数据、类型定义和受影响页面。
- 媒体长连接 writer 不持有 `media_source` 内部锁写 socket。

命名修改：

- `http_service` -> `http`。
- `http_console_service.h` 可改为 `http_console.h`。
- `HttpStreamWriter` 根据职责移动到 `http_media`，命名为 `HttpMediaWriter`、
  `FlvHttpWriter`、`MjpegHttpWriter` 或 `HlsHttpHandler`。
- `WebrtcHttpHandler` 保留 HTTP handler 语义，但依赖 `IWebrtc` 而不是
  `IWebrtcService`。

框架修改：

- `http` 只负责路由和普通响应。
- `http_media` 负责从 `media_source` reader 取数据并写入 `net` session。
- WebRTC signaling 可先留在 HTTP 路由层，但实现类归 `http_media`，避免污染控制 API。

意义：

- 控制 API 不再被媒体长连接阻塞。
- HTTP-FLV/HLS/MJPEG 的慢客户端策略可以统一治理。
- Web Console 调用跟随新 API 一次性迁移，内部结构更清晰。

验收：

- 控制 API 不依赖媒体长连接 writer。
- FLV、HLS、MJPEG 和 WebRTC signaling 通过 `http_media` 输出。
- Web Console 能按新 HTTP API 构建并访问受影响功能。
- 断开浏览器播放连接后 reader count 回落。

### 任务 8：清理 `media_codec` 和 `media_mux`

- 推荐 Codex：Codex-3 Codec/RTP。
- 前置依赖：任务 3 完成。
- 可并行任务：任务 4、5、6；接口评审后可与任务 7 并行。
- 禁止同时修改：RTSP/WebRTC Codex 不得同时自建 H264/H265 parser 或 RTP packetizer。

工作内容：

- 将 `stream_codec` 重命名为 `media_codec`。
- 只保留 H264/H265 parser、SPS/PPS/VPS、IDR 判断、AnnexB/AVCC 转换等 codec 工具。
- 将 `stream_mux` 先改为 `media_mux`，再把 FLV/HLS/RTP 逐步拆入对应协议模块。
- 删除只为旧接口转调的 mux wrapper。

命名修改：

- `stream_codec` -> `media_codec`。
- `stream_mux` -> `media_mux`，过渡完成后删除。
- H264/H265 相关文件命名为 `h264_parser.*`、`h265_parser.*`、`annexb_reader.*`、
  `avcc_writer.*`。
- FLV 文件命名归 `flv_muxer.*`，HLS 文件命名归 `hls_maker.*`，RTP 文件命名归
  `rtp_packetizer.*`。

框架修改：

- codec parser 不依赖 HTTP/RTSP/WebRTC。
- FLV/HLS/RTP mux 不再集中在一个泛名模块中。
- 协议模块通过明确的 codec utils 处理参数集和 NAL。

意义：

- 解决 `stream_mux` 职责过宽和命名含混。
- 协议实现更容易定位，减少跨模块循环依赖。

验收：

- 生产代码不再 include `stream_codec` 或 `stream_mux`。
- `media_codec` 不依赖协议模块。
- `media_mux` 最终只剩空迁移壳或被删除。

### 任务 9：重构 RTSP 为 session/muxer/packetizer/transport

- 推荐 Codex：Codex-5 RTSP。
- 前置依赖：任务 5、任务 6、任务 8。
- 可并行任务：任务 7、任务 10、任务 11。
- 禁止同时修改：不要改 `media_source` reader、`net` session 或共享 RTP packetizer 公共接口。

工作内容：

- 将 `rtsp_service` 重命名为 `rtsp`。
- `RtspSession` 处理 OPTIONS/DESCRIBE/SETUP/PLAY/TEARDOWN、认证、timeout。
- `RtspMuxer` 生成 SDP，并根据 `MediaTrack` 建立 RTP 输出。
- `RtpPacketizer` 负责 H264/H265 RTP 分片、seq、ssrc、timestamp、marker。
- `RtpTransport` 负责 TCP interleaved 和 UDP 发送、RTCP 基础处理。

命名修改：

- `IRtspService` -> `IRtsp`。
- `RtspServiceOptions` -> `RtspOptions`。
- `RtspServiceStats` -> `RtspStats`。
- `CreateRtspService()` -> `CreateRtsp()`。
- 当前 `rtsp_request_handler.*` 如只处理 request dispatch，可命名为
  `rtsp_request_dispatcher.*`；若承载 session 行为，应并入 `rtsp_session.*`。

框架修改：

- RTSP 不再拥有 GOP cache。
- RTSP 通过 `media_source` reader 获取帧，通过 `RtpPacketizer` 输出 RTP。
- TCP/UDP transport 与 `RtspSession` 生命周期绑定，断线时 detach reader。

意义：

- 职责贴近 ZLMediaKit，RTSP 行为更容易验证。
- TCP/UDP 共享 packetizer，减少两套 RTP 逻辑。

验收：

- VLC/ffplay RTSP TCP/UDP 均可拉流。
- DESCRIBE SDP 与当前 codec/track 一致。
- PLAY 后等待关键帧，不发送不可解码 GOP 前帧。
- TEARDOWN/断线后 reader count 回落。

### 任务 10：替换 WebRTC 为 native 实现

- 推荐 Codex：Codex-6 WebRTC。
- 前置依赖：任务 5、任务 6、任务 8；任务 3 已稳定 `webrtc` public API。
- 可并行任务：任务 7、任务 9、任务 11；WebRTC 内部 SDP/ICE/DTLS/SRTP 子任务可在同一分支内并行。
- 禁止同时修改：不要改 `media_source`、`net`、`media_codec` 公共接口；不要和全局 rename 同时执行。

工作内容：

- 将 `webrtc_service` 重命名为 `webrtc`。
- 删除 metaRTC/Yang engine、pacer、callback 和链接项。
- 新增 SDP、STUN、ICE、DTLS、SRTP、WebRTC transport、WebRTC session、
  WebRTC RTP sender。
- 第一版只做 video-only sendonly，优先 H264。
- 按 native WebRTC 重新定义 HTTP signaling 契约，并同步更新 `http_media` 和 Web Console。
- 以 ZLMediaKit WebRTC 实现为标准：`WebRtcSession` 负责 HTTP/WHEP 类信令入口，
  `WebRtcTransport` 组合 SDP、ICE、DTLS、SRTP、RTCP 和 socket 输出，底层由
  `Sdp`、`IceTransport`、`DtlsTransport`、`SrtpSession`、`StunPacket` 等文件支撑。

命名修改：

- `IWebrtcService` -> `IWebrtc`。
- `WebrtcServiceOptions` -> `WebrtcOptions`。
- `WebrtcServiceDependencies` -> `WebrtcDependencies`。
- `WebrtcServiceStats` -> `WebrtcStats`。
- `CreateWebrtcService()` -> `CreateWebrtc()`。
- 删除 `BackendName()`。
- 删除 `MetaRtc*`、`Yang*` 命名。
- 文件改为 `webrtc_session.*`、`webrtc_transport.*`、`webrtc_sdp.*`、
  `ice_transport.*`、`dtls_transport.*`、`srtp_session.*`、`stun_packet.*`、
  `webrtc_rtp_sender.*`。

框架修改：

- WebRTC 从“外部 SDK engine”改为本工程 native transport。
- 数据流为 `media_source reader -> webrtc_rtp_sender -> srtp_session -> ice_transport`。
- ICE/DTLS/SRTP 状态由 `webrtc_transport` 管理。
- PLI 触发 `media_source.RequestKeyFrame()`。
- `http_media` 只负责 WebRTC signaling DTO 和 HTTP response，不拥有
  ICE/DTLS/SRTP 状态。
- `webrtc` 不直接拥有设备编码入口，只通过 `media_source` reader 取帧。

子任务：

1. **10.1 构建和依赖清理**
   - 从根 `Makefile` 和 WebRTC 模块 `Makefile` 删除 `metaRTC_src` include。
   - 删除 `libmetartc8.a`、`libmetartccore8.a`、`libyangutil8.a` 链接项。
   - 保留 OpenSSL 和 libsrtp；usrsctp/datachannel 首版不启用。
   - 验收：链接日志不再出现 metaRTC/Yang 库，生产代码不再 include `yangrtc/*`。

2. **10.2 public API 收敛**
   - `IWebrtcService` 改为 `IWebrtc`，`CreateWebrtcService()` 改为 `CreateWebrtc()`。
   - 删除 `BackendName()`；stats 只暴露 `enabled`、`active_peers`、ICE/DTLS/SRTP
     必要状态和帧发送/丢弃统计。
   - HTTP request/response JSON 按 native WebRTC 状态和错误模型重构。
   - 验收：HTTP handler 只依赖 `IWebrtc`，前端 signaling 调用已同步迁移到新契约。

3. **10.3 SDP 层**
   - 参考 ZLM `Sdp.h/Sdp.cpp`，实现 offer parse 和 answer generate。
   - 支持 video-only sendonly：`m=video`、`rtpmap`、`fmtp`、`rtcp-fb`、`mid`、
     `ice-ufrag`、`ice-pwd`、`fingerprint`、`setup`、candidate。
   - 首版优先 H264，H265 只在浏览器能力和本工程 codec 参数明确时开启。
   - 验收：Chrome/Edge 发来的 offer 可解析，生成 answer 后浏览器接受 SDP。

4. **10.4 STUN/ICE 层**
   - 参考 ZLM `StunPacket`、`IceTransport`，实现 binding request/response、
     USERNAME、MESSAGE-INTEGRITY、FINGERPRINT、USE-CANDIDATE、PRIORITY。
   - 首版实现 UDP host candidate 和 selected pair；TURN、relay、TCP candidate 后置。
   - ICE ufrag/pwd 由 peer session 生成并写入 SDP。
   - 验收：浏览器 ICE state 到 connected/completed，selected pair 可统计。

5. **10.5 DTLS 层**
   - 参考 ZLM `DtlsTransport`，用 OpenSSL 生成证书、计算 fingerprint、执行 DTLS
     server role handshake。
   - 校验 remote fingerprint，导出 SRTP key。
   - 不使用 C++ exception；所有失败返回 `bool` 或状态枚举，并关闭 peer。
   - 验收：浏览器 DTLS state connected，失败路径能释放 ICE pair 和 timer。

6. **10.6 SRTP/RTCP 层**
   - 参考 ZLM `SrtpSession`，用 libsrtp 建立 outbound SRTP context。
   - RTP/RTCP 加密后通过 selected ICE pair 发送。
   - 首版处理 PLI/FIR 触发关键帧请求；NACK 和 TWCC 可先保留结构，基础播放稳定后补。
   - 验收：浏览器能收到可解密 RTP；收到 PLI 后本工程请求关键帧。

7. **10.7 RTP sender 层**
   - 复用或对齐 RTSP 的 `RtpPacketizer`，不要 WebRTC 单独写一套 H264/H265 分片逻辑。
   - 从 `media_source` reader 获取 corrected timestamp 和 keyframe-first 帧。
   - 维护 ssrc、seq、rtp timestamp、marker bit、payload type。
   - 验收：首帧从关键帧开始，长时间播放 RTP timestamp 单调，断线后 reader count 回落。

8. **10.8 session 生命周期和 HTTP signaling**
   - `WebrtcSession` 管 peer id、stream id、SDP、ICE candidate、transport、reader、
     timeout、close。
   - `http_media` 的 WebRTC handler 只做 create peer、offer、candidate、close 的
     JSON 转换和鉴权。
   - peer close 时按顺序 detach reader、关闭 RTP sender、关闭 SRTP、关闭 DTLS、
     释放 ICE endpoint/timer。
   - 验收：频繁创建/关闭 peer 后 fd、timer、reader count、RSS 回落。

并行方式：

- 10.1 和 10.2 可由一人先做，作为 WebRTC 分支基线。
- 10.3 SDP、10.4 STUN/ICE、10.5 DTLS、10.6 SRTP 可在接口约定后并行，但必须共同
  对齐 `webrtc_transport` 的状态枚举和错误返回。
- 10.7 等任务 5 的 `media_source` reader 和任务 8/9 的 RTP packetizer 接口稳定后再接。
- 10.8 等 10.3-10.7 基础链路可跑通后再收口，避免 session 里堆临时状态。

意义：

- 去掉 metaRTC 体积、构建和行为黑盒。
- WebRTC 与 RTSP 共享 RTP packetizer/时间戳/关键帧模型。
- 后续可按需补 NACK/TWCC/TURN，而不是被外部 SDK 牵制。

验收：

- 根 `Makefile` 不再链接 `libmetartc8.a`、`libmetartccore8.a`、`libyangutil8.a`。
- 生产代码不再 include `yangrtc/*`。
- 浏览器 WebRTC 能完成 offer/answer、ICE、DTLS、SRTP 收帧。
- 关闭 peer 后 reader、UDP endpoint、timer、SRTP context 释放。

### 任务 11：ONVIF 边界整理

- 推荐 Codex：Codex-7 ONVIF。
- 前置依赖：任务 3；RTSP URL 契约来自任务 9。
- 可并行任务：任务 7、任务 9、任务 10。
- 禁止同时修改：不要接触媒体热路径、RTP packetizer、WebRTC transport 或 `media_source` reader。

工作内容：

- 将 `onvif_service` 重命名为 `onvif`。
- 保留 discovery、device、media/profile、PTZ 等 ONVIF 业务壳。
- RTSP URL 只从 `rtsp` 模块读取 listen address 和 stream path。
- ONVIF 不直接接触 RTP packetizer、media reader 或 WebRTC 状态。

命名修改：

- public header 从 `onvif_service.h` 收敛到 `onvif.h` 或现有 `onvif_server.h`。
- `OnvifDeviceService`、`OnvifMediaService` 如果表达 ONVIF 规范 service，可以保留，
  因为这里的 `Service` 是 ONVIF 协议概念，不是模块后缀。
- 变量 `onvif_service` -> `onvif`。

框架修改：

- ONVIF 依赖 auth/config/system/time/device info 和 RTSP URL，不依赖媒体热路径。
- ONVIF media profile 输出 URL 指向 `rtsp`。

意义：

- 避免把 ONVIF 协议壳误拆进媒体核心。
- ONVIF 和 RTSP 可以并行开发，只通过 URL 契约集成。

验收：

- ONVIF 客户端可发现设备。
- 返回 RTSP URL 可被 VLC/ffplay 播放。
- ONVIF 修改不影响 HTTP-FLV/HLS/WebRTC。

### 任务 12：最终清理和质量门禁

- 推荐 Codex：Codex-8 Cleanup。
- 前置依赖：任务 2-11 全部完成并通过对应验收。
- 可并行任务：无；这是最终收口任务。
- 禁止同时修改：其他 Codex 不得同时删除旧目录、旧 public header 或旧库名。

工作内容：

- 删除 `stream_hub_service`。
- 删除旧 public header、旧 include path、旧库名和旧 Makefile 入口。
- 删除临时 wrapper、兼容 typedef、只转调 helper。
- 全工程扫描旧命名和旧第三方依赖。
- 更新文档、Web mock、状态展示和发布说明。

命名修改：

- 生产代码不得残留 `stream_hub_service`、`stream_mux`、`stream_codec`。
- 生产代码不得残留 `webrtc_service`、`MetaRtc`、`Yang`。
- 允许历史迁移文档保留旧名称，但必须标注为旧名称。

框架修改：

- 最终依赖图固定为：
  `device_media -> media_pipeline -> media_source -> http_media/rtsp/webrtc`。
- `http` 负责控制 API，`http_media` 负责媒体 HTTP。
- `net` 负责所有长连接和 UDP endpoint 基础能力。

意义：

- 清掉所有“双路径”和“旧名兼容”，降低后续维护成本。
- 确保重构不是只改名字，而是实际收敛边界和资源模型。

验收：

- `rg "_service|webrtc_service|metaRTC|MetaRtc|Yang|stream_hub|stream_mux|stream_codec"`
  只允许历史文档或迁移说明中出现。
- `make -j2`、关键模块构建和 Web build 通过。
- 多协议播放、慢客户端断开、频繁连接断开后的资源回落通过验证。

## 验证计划

任务 3 后的当前基础验证：

- `make -j2`
- `make -C libs/media_source`
- `make -C libs/media_pipeline`
- `make -C libs/net`
- `make -C libs/http`
- `make -C libs/rtsp`
- `make -C libs/webrtc`

按任务追加验证：

- 任务 7 创建 `http_media` 后，增加 `make -C libs/http_media`。
- 任务 7 修改 Web Console 后，在 `www/` 执行 `npm run build`。
- 任务 8 修改 codec/RTP 边界后，增加 `make -C libs/media_codec` 和
  `make -C libs/media_mux`，直到 `media_mux` 被拆除。
- 任务 10 清理 WebRTC 依赖后，检查链接日志不再出现 metaRTC/Yang 库。

播放：

- 浏览器 HTTP-FLV 首屏、低延迟、断线重连。
- 浏览器 HLS 连续播放、刷新、切换码流。
- VLC/ffplay RTSP TCP/UDP 拉流。
- ONVIF 客户端发现设备后，通过返回的 RTSP URL 播放。
- WebRTC native 首帧、ICE connected、DTLS connected、SRTP RTP 收帧、断线恢复。

压测和清理：

- 单路多客户端 HLS/FLV/RTSP/WebRTC 同时拉流。
- 慢客户端发送队列触顶后主动断开。
- 频繁连接/断开后 RSS、fd 数、reader count、帧缓存数量回落。
- 长时间播放检查 PTS/DTS 单调、无花屏、无 segment 空洞。
- `rg "_service|webrtc_service|metaRTC|MetaRtc|Yang|stream_hub|stream_mux|stream_codec"`
  只允许历史文档或迁移说明中出现。
- 链接日志确认不再链接 metaRTC 相关库。
